# Database Schema

The schema is auto-generated by Django migrations. The most important tables:

## accounts

| Table | Key columns |
|---|---|
| `accounts_user` | `id (uuid)`, `email (unique)`, `username (unique)`, `phone`, `role`, `is_active`, `is_staff`, `email_verified_at`, `last_login_ip`, timestamps |
| `accounts_address` | `user_id`, `full_name`, `phone`, `line1/2`, `city/state/postal_code/country`, default flags |
| `accounts_emailotp` | `user_id`, `code`, `purpose`, `expires_at`, `consumed` |
| `accounts_wishlist` | `user_id (1-1)`, M2M `products` |

## catalog

| Table | Key columns |
|---|---|
| `catalog_brand` | `id (uuid)`, `name (unique)`, `slug (unique)`, `country_of_origin`, `founded_year` |
| `catalog_category` | `id (uuid)`, `name (unique)`, `slug (unique)`, `parent_id (FK self)` |
| `catalog_product` | `id (uuid)`, `name`, `slug (unique)`, `sku (unique)`, `price`, `sale_price`, `stock_quantity`, `volume_ml`, `concentration`, `gender`, `fragrance_family`, `top_notes/heart_notes/base_notes (jsonb)`, `brand_id (FK)`, M2M `categories`, `average_rating`, `review_count`, `sales_count`, flags, meta |
| `catalog_productimage` | `product_id (FK)`, `image`, `is_primary`, `display_order` |
| `catalog_review` | `product_id`, `user_id`, `rating 1-5`, `is_verified_purchase`, `is_approved` |

## orders

| Table | Key columns |
|---|---|
| `orders_coupon` | `code (unique)`, `discount_type`, `discount_value`, `min_order_value`, `max_discount`, `usage_limit`, `usage_count`, `starts_at`, `expires_at` |
| `orders_couponredemption` | `coupon_id`, `user_id`, `order_id` |
| `orders_cart` | `user_id (1-1)`, `session_key`, `coupon_id (FK)` |
| `orders_cartitem` | `cart_id`, `product_id`, `quantity`, `saved_for_later` |
| `orders_order` | `id (uuid)`, `order_number (unique)`, `user_id`, `status`, `payment_status`, shipping/billing address, monetary fields, `razorpay_order_id`, `paid_at`, `delivered_at` |
| `orders_orderitem` | `order_id`, `product_id (nullable)`, snapshot of name/sku/image/price |
| `orders_ordertimeline` | `order_id`, `status`, `note`, `created_at` |

## payments

Payments are stateless — Razorpay order/payment identifiers are stored on the
`orders_order` row. No additional tables required for v1.

## Migrations

```bash
python manage.py makemigrations
python manage.py migrate
```

## Performance indexes (auto-created)

- `catalog_product`: composite indexes on `(is_active, is_featured)`, `(is_active, is_bestseller)`, `(is_active, is_new_arrival)`, `(is_active, price)`, `(brand, is_active)`, `(fragrance_family, is_active)`, `(gender, is_active)`.
- `catalog_review`: `(product, is_approved)`.
- `orders_order`: `(user, status)`, `(status, created_at)`.

## Trigram search

The Postgres extensions `pg_trgm` and `unaccent` are enabled at container
initialization. The `SearchView` performs a trigram-friendly Q-search on
product names, descriptions, brand names, and JSON note fields.
